def character_counter(str1):
    dict = {}
    for x in str1:
        char = dict.char()
        if x in char:
         dict[x] += 1
        else:
         dict[x] = 1
    return dict

string = input("Enter your word or scentance: ")
print(character_freq(string))
